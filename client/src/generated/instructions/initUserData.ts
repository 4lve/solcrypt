/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findUserAccountPda } from "../pdas";
import { SOLCRYPT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INIT_USER_DATA_DISCRIMINATOR = 1;

export function getInitUserDataDiscriminatorBytes() {
  return getU8Encoder().encode(INIT_USER_DATA_DISCRIMINATOR);
}

export type InitUserDataInstruction<
  TProgram extends string = typeof SOLCRYPT_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitUserDataInstructionData = {
  discriminator: number;
  x25519Pubkey: Array<number>;
};

export type InitUserDataInstructionDataArgs = {
  discriminator?: number;
  x25519Pubkey: Array<number>;
};

export function getInitUserDataInstructionDataEncoder(): FixedSizeEncoder<InitUserDataInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["x25519Pubkey", getArrayEncoder(getU8Encoder(), { size: 32 })],
    ]),
    (value) => ({
      ...value,
      discriminator: value.discriminator ?? INIT_USER_DATA_DISCRIMINATOR,
    }),
  );
}

export function getInitUserDataInstructionDataDecoder(): FixedSizeDecoder<InitUserDataInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["x25519Pubkey", getArrayDecoder(getU8Decoder(), { size: 32 })],
  ]);
}

export function getInitUserDataInstructionDataCodec(): FixedSizeCodec<
  InitUserDataInstructionDataArgs,
  InitUserDataInstructionData
> {
  return combineCodec(
    getInitUserDataInstructionDataEncoder(),
    getInitUserDataInstructionDataDecoder(),
  );
}

export type InitUserDataAsyncInput<
  TAccountSigner extends string = string,
  TAccountUserAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userAccount?: Address<TAccountUserAccount>;
  systemProgram: Address<TAccountSystemProgram>;
  discriminator?: InitUserDataInstructionDataArgs["discriminator"];
  x25519Pubkey: InitUserDataInstructionDataArgs["x25519Pubkey"];
};

export async function getInitUserDataInstructionAsync<
  TAccountSigner extends string,
  TAccountUserAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCRYPT_PROGRAM_ADDRESS,
>(
  input: InitUserDataAsyncInput<
    TAccountSigner,
    TAccountUserAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitUserDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCRYPT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userAccount.value) {
    accounts.userAccount.value = await findUserAccountPda({
      userPubkey: expectAddress(accounts.signer.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitUserDataInstructionDataEncoder().encode(
      args as InitUserDataInstructionDataArgs,
    ),
    programAddress,
  } as InitUserDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount,
    TAccountSystemProgram
  >);
}

export type InitUserDataInput<
  TAccountSigner extends string = string,
  TAccountUserAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userAccount: Address<TAccountUserAccount>;
  systemProgram: Address<TAccountSystemProgram>;
  discriminator?: InitUserDataInstructionDataArgs["discriminator"];
  x25519Pubkey: InitUserDataInstructionDataArgs["x25519Pubkey"];
};

export function getInitUserDataInstruction<
  TAccountSigner extends string,
  TAccountUserAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCRYPT_PROGRAM_ADDRESS,
>(
  input: InitUserDataInput<
    TAccountSigner,
    TAccountUserAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitUserDataInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountUserAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCRYPT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitUserDataInstructionDataEncoder().encode(
      args as InitUserDataInstructionDataArgs,
    ),
    programAddress,
  } as InitUserDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount,
    TAccountSystemProgram
  >);
}

export type ParsedInitUserDataInstruction<
  TProgram extends string = typeof SOLCRYPT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    userAccount: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitUserDataInstructionData;
};

export function parseInitUserDataInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitUserDataInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      userAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitUserDataInstructionDataDecoder().decode(instruction.data),
  };
}
