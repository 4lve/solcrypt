/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findUserAccountPda } from "../pdas";
import { SOLCRYPT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REMOVE_THREAD_DATA_DISCRIMINATOR = 4;

export function getRemoveThreadDataDiscriminatorBytes() {
  return getU8Encoder().encode(REMOVE_THREAD_DATA_DISCRIMINATOR);
}

export type RemoveThreadDataInstruction<
  TProgram extends string = typeof SOLCRYPT_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveThreadDataInstructionData = {
  discriminator: number;
  threadId: Array<number>;
};

export type RemoveThreadDataInstructionDataArgs = {
  discriminator?: number;
  threadId: Array<number>;
};

export function getRemoveThreadDataInstructionDataEncoder(): FixedSizeEncoder<RemoveThreadDataInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["threadId", getArrayEncoder(getU8Encoder(), { size: 32 })],
    ]),
    (value) => ({
      ...value,
      discriminator: value.discriminator ?? REMOVE_THREAD_DATA_DISCRIMINATOR,
    }),
  );
}

export function getRemoveThreadDataInstructionDataDecoder(): FixedSizeDecoder<RemoveThreadDataInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["threadId", getArrayDecoder(getU8Decoder(), { size: 32 })],
  ]);
}

export function getRemoveThreadDataInstructionDataCodec(): FixedSizeCodec<
  RemoveThreadDataInstructionDataArgs,
  RemoveThreadDataInstructionData
> {
  return combineCodec(
    getRemoveThreadDataInstructionDataEncoder(),
    getRemoveThreadDataInstructionDataDecoder(),
  );
}

export type RemoveThreadDataAsyncInput<
  TAccountSigner extends string = string,
  TAccountUserAccount extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userAccount?: Address<TAccountUserAccount>;
  discriminator?: RemoveThreadDataInstructionDataArgs["discriminator"];
  threadId: RemoveThreadDataInstructionDataArgs["threadId"];
};

export async function getRemoveThreadDataInstructionAsync<
  TAccountSigner extends string,
  TAccountUserAccount extends string,
  TProgramAddress extends Address = typeof SOLCRYPT_PROGRAM_ADDRESS,
>(
  input: RemoveThreadDataAsyncInput<TAccountSigner, TAccountUserAccount>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RemoveThreadDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCRYPT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userAccount.value) {
    accounts.userAccount.value = await findUserAccountPda({
      userPubkey: expectAddress(accounts.signer.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userAccount),
    ],
    data: getRemoveThreadDataInstructionDataEncoder().encode(
      args as RemoveThreadDataInstructionDataArgs,
    ),
    programAddress,
  } as RemoveThreadDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount
  >);
}

export type RemoveThreadDataInput<
  TAccountSigner extends string = string,
  TAccountUserAccount extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userAccount: Address<TAccountUserAccount>;
  discriminator?: RemoveThreadDataInstructionDataArgs["discriminator"];
  threadId: RemoveThreadDataInstructionDataArgs["threadId"];
};

export function getRemoveThreadDataInstruction<
  TAccountSigner extends string,
  TAccountUserAccount extends string,
  TProgramAddress extends Address = typeof SOLCRYPT_PROGRAM_ADDRESS,
>(
  input: RemoveThreadDataInput<TAccountSigner, TAccountUserAccount>,
  config?: { programAddress?: TProgramAddress },
): RemoveThreadDataInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountUserAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCRYPT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userAccount),
    ],
    data: getRemoveThreadDataInstructionDataEncoder().encode(
      args as RemoveThreadDataInstructionDataArgs,
    ),
    programAddress,
  } as RemoveThreadDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount
  >);
}

export type ParsedRemoveThreadDataInstruction<
  TProgram extends string = typeof SOLCRYPT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    userAccount: TAccountMetas[1];
  };
  data: RemoveThreadDataInstructionData;
};

export function parseRemoveThreadDataInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveThreadDataInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { signer: getNextAccount(), userAccount: getNextAccount() },
    data: getRemoveThreadDataInstructionDataDecoder().decode(instruction.data),
  };
}
