/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findUserAccountPda } from "../pdas";
import { SOLCRYPT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const ACCEPT_THREAD_DATA_DISCRIMINATOR = 3;

export function getAcceptThreadDataDiscriminatorBytes() {
  return getU8Encoder().encode(ACCEPT_THREAD_DATA_DISCRIMINATOR);
}

export type AcceptThreadDataInstruction<
  TProgram extends string = typeof SOLCRYPT_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      ...TRemainingAccounts,
    ]
  >;

export type AcceptThreadDataInstructionData = {
  discriminator: number;
  threadId: Array<number>;
};

export type AcceptThreadDataInstructionDataArgs = {
  discriminator?: number;
  threadId: Array<number>;
};

export function getAcceptThreadDataInstructionDataEncoder(): FixedSizeEncoder<AcceptThreadDataInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["threadId", getArrayEncoder(getU8Encoder(), { size: 32 })],
    ]),
    (value) => ({
      ...value,
      discriminator: value.discriminator ?? ACCEPT_THREAD_DATA_DISCRIMINATOR,
    }),
  );
}

export function getAcceptThreadDataInstructionDataDecoder(): FixedSizeDecoder<AcceptThreadDataInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["threadId", getArrayDecoder(getU8Decoder(), { size: 32 })],
  ]);
}

export function getAcceptThreadDataInstructionDataCodec(): FixedSizeCodec<
  AcceptThreadDataInstructionDataArgs,
  AcceptThreadDataInstructionData
> {
  return combineCodec(
    getAcceptThreadDataInstructionDataEncoder(),
    getAcceptThreadDataInstructionDataDecoder(),
  );
}

export type AcceptThreadDataAsyncInput<
  TAccountSigner extends string = string,
  TAccountUserAccount extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userAccount?: Address<TAccountUserAccount>;
  discriminator?: AcceptThreadDataInstructionDataArgs["discriminator"];
  threadId: AcceptThreadDataInstructionDataArgs["threadId"];
};

export async function getAcceptThreadDataInstructionAsync<
  TAccountSigner extends string,
  TAccountUserAccount extends string,
  TProgramAddress extends Address = typeof SOLCRYPT_PROGRAM_ADDRESS,
>(
  input: AcceptThreadDataAsyncInput<TAccountSigner, TAccountUserAccount>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AcceptThreadDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCRYPT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userAccount.value) {
    accounts.userAccount.value = await findUserAccountPda({
      userPubkey: expectAddress(accounts.signer.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userAccount),
    ],
    data: getAcceptThreadDataInstructionDataEncoder().encode(
      args as AcceptThreadDataInstructionDataArgs,
    ),
    programAddress,
  } as AcceptThreadDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount
  >);
}

export type AcceptThreadDataInput<
  TAccountSigner extends string = string,
  TAccountUserAccount extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userAccount: Address<TAccountUserAccount>;
  discriminator?: AcceptThreadDataInstructionDataArgs["discriminator"];
  threadId: AcceptThreadDataInstructionDataArgs["threadId"];
};

export function getAcceptThreadDataInstruction<
  TAccountSigner extends string,
  TAccountUserAccount extends string,
  TProgramAddress extends Address = typeof SOLCRYPT_PROGRAM_ADDRESS,
>(
  input: AcceptThreadDataInput<TAccountSigner, TAccountUserAccount>,
  config?: { programAddress?: TProgramAddress },
): AcceptThreadDataInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountUserAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCRYPT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userAccount),
    ],
    data: getAcceptThreadDataInstructionDataEncoder().encode(
      args as AcceptThreadDataInstructionDataArgs,
    ),
    programAddress,
  } as AcceptThreadDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount
  >);
}

export type ParsedAcceptThreadDataInstruction<
  TProgram extends string = typeof SOLCRYPT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    userAccount: TAccountMetas[1];
  };
  data: AcceptThreadDataInstructionData;
};

export function parseAcceptThreadDataInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAcceptThreadDataInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { signer: getNextAccount(), userAccount: getNextAccount() },
    data: getAcceptThreadDataInstructionDataDecoder().decode(instruction.data),
  };
}
