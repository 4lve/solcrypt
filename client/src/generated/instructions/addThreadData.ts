/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findUserAccountPda } from "../pdas";
import { SOLCRYPT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const ADD_THREAD_DATA_DISCRIMINATOR = 2;

export function getAddThreadDataDiscriminatorBytes() {
  return getU8Encoder().encode(ADD_THREAD_DATA_DISCRIMINATOR);
}

export type AddThreadDataInstruction<
  TProgram extends string = typeof SOLCRYPT_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddThreadDataInstructionData = {
  discriminator: number;
  threadId: Array<number>;
  state: number;
};

export type AddThreadDataInstructionDataArgs = {
  discriminator?: number;
  threadId: Array<number>;
  state: number;
};

export function getAddThreadDataInstructionDataEncoder(): FixedSizeEncoder<AddThreadDataInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["threadId", getArrayEncoder(getU8Encoder(), { size: 32 })],
      ["state", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: value.discriminator ?? ADD_THREAD_DATA_DISCRIMINATOR,
    }),
  );
}

export function getAddThreadDataInstructionDataDecoder(): FixedSizeDecoder<AddThreadDataInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["threadId", getArrayDecoder(getU8Decoder(), { size: 32 })],
    ["state", getU8Decoder()],
  ]);
}

export function getAddThreadDataInstructionDataCodec(): FixedSizeCodec<
  AddThreadDataInstructionDataArgs,
  AddThreadDataInstructionData
> {
  return combineCodec(
    getAddThreadDataInstructionDataEncoder(),
    getAddThreadDataInstructionDataDecoder(),
  );
}

export type AddThreadDataAsyncInput<
  TAccountSigner extends string = string,
  TAccountUserAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userAccount?: Address<TAccountUserAccount>;
  systemProgram: Address<TAccountSystemProgram>;
  discriminator?: AddThreadDataInstructionDataArgs["discriminator"];
  threadId: AddThreadDataInstructionDataArgs["threadId"];
  state: AddThreadDataInstructionDataArgs["state"];
};

export async function getAddThreadDataInstructionAsync<
  TAccountSigner extends string,
  TAccountUserAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCRYPT_PROGRAM_ADDRESS,
>(
  input: AddThreadDataAsyncInput<
    TAccountSigner,
    TAccountUserAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddThreadDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCRYPT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userAccount.value) {
    accounts.userAccount.value = await findUserAccountPda({
      userPubkey: expectAddress(accounts.signer.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddThreadDataInstructionDataEncoder().encode(
      args as AddThreadDataInstructionDataArgs,
    ),
    programAddress,
  } as AddThreadDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount,
    TAccountSystemProgram
  >);
}

export type AddThreadDataInput<
  TAccountSigner extends string = string,
  TAccountUserAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userAccount: Address<TAccountUserAccount>;
  systemProgram: Address<TAccountSystemProgram>;
  discriminator?: AddThreadDataInstructionDataArgs["discriminator"];
  threadId: AddThreadDataInstructionDataArgs["threadId"];
  state: AddThreadDataInstructionDataArgs["state"];
};

export function getAddThreadDataInstruction<
  TAccountSigner extends string,
  TAccountUserAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCRYPT_PROGRAM_ADDRESS,
>(
  input: AddThreadDataInput<
    TAccountSigner,
    TAccountUserAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddThreadDataInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountUserAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCRYPT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddThreadDataInstructionDataEncoder().encode(
      args as AddThreadDataInstructionDataArgs,
    ),
    programAddress,
  } as AddThreadDataInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount,
    TAccountSystemProgram
  >);
}

export type ParsedAddThreadDataInstruction<
  TProgram extends string = typeof SOLCRYPT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    userAccount: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: AddThreadDataInstructionData;
};

export function parseAddThreadDataInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddThreadDataInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      userAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddThreadDataInstructionDataDecoder().decode(instruction.data),
  };
}
