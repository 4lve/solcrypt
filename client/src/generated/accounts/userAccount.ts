/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
} from "@solana/kit";
import { findUserAccountPda, UserAccountSeeds } from "../pdas";
import {
  getThreadEntryDecoder,
  getThreadEntryEncoder,
  type ThreadEntry,
  type ThreadEntryArgs,
} from "../types";

export const USER_ACCOUNT_DISCRIMINATOR = 0;

export function getUserAccountDiscriminatorBytes() {
  return getU8Encoder().encode(USER_ACCOUNT_DISCRIMINATOR);
}

export type UserAccount = {
  discriminator: number;
  x25519Pubkey: Array<number>;
  threads: Array<ThreadEntry>;
};

export type UserAccountArgs = {
  discriminator?: number;
  x25519Pubkey: Array<number>;
  threads: Array<ThreadEntryArgs>;
};

/** Gets the encoder for {@link UserAccountArgs} account data. */
export function getUserAccountEncoder(): Encoder<UserAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["x25519Pubkey", getArrayEncoder(getU8Encoder(), { size: 32 })],
      ["threads", getArrayEncoder(getThreadEntryEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: value.discriminator ?? USER_ACCOUNT_DISCRIMINATOR,
    }),
  );
}

/** Gets the decoder for {@link UserAccount} account data. */
export function getUserAccountDecoder(): Decoder<UserAccount> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["x25519Pubkey", getArrayDecoder(getU8Decoder(), { size: 32 })],
    ["threads", getArrayDecoder(getThreadEntryDecoder())],
  ]);
}

/** Gets the codec for {@link UserAccount} account data. */
export function getUserAccountCodec(): Codec<UserAccountArgs, UserAccount> {
  return combineCodec(getUserAccountEncoder(), getUserAccountDecoder());
}

export function decodeUserAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<UserAccount, TAddress>;
export function decodeUserAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<UserAccount, TAddress>;
export function decodeUserAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<UserAccount, TAddress> | MaybeAccount<UserAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getUserAccountDecoder(),
  );
}

export async function fetchUserAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<UserAccount, TAddress>> {
  const maybeAccount = await fetchMaybeUserAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeUserAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<UserAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeUserAccount(maybeAccount);
}

export async function fetchAllUserAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<UserAccount>[]> {
  const maybeAccounts = await fetchAllMaybeUserAccount(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeUserAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<UserAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeUserAccount(maybeAccount));
}

export async function fetchUserAccountFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: UserAccountSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<UserAccount>> {
  const maybeAccount = await fetchMaybeUserAccountFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeUserAccountFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: UserAccountSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<UserAccount>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findUserAccountPda(seeds, { programAddress });
  return await fetchMaybeUserAccount(rpc, address, fetchConfig);
}
